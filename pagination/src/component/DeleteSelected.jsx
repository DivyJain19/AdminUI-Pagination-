import React from "react";
import classes from "./DeleteSelected.module.css";
const DeleteSelected = () => {
  return <button className={classes.dltSelected}>Delete Selected</button>;
};

export default DeleteSelected;
